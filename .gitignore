node_modules
dist
.test-build
.venv/
__pycache__/
.pytest_cache/
*.pyc
*.tsbuildinfo
.DS_Store

# Local secrets. Commit only the documented example file.
.env
.env.*
!.env.example

# Raw GIS source files are large and are only used to generate app assets.
map-data-input/

# Local deployment metadata and generated presentation artifacts.
.vercel/
outputs/

# 축제 효과 분석 원본 데이터(방문자 일별 CSV 등). scripts/festival-effect/README.md 참고.
scripts/festival-effect/data/
